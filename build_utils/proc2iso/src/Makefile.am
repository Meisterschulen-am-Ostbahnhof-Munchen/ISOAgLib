bin_PROGRAMS = proc2iso
proc2iso_SOURCES = proc2iso.cpp

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
proc2iso_LDFLAGS = $(all_libraries) $(LIBSDL_RPATH)
proc2iso_CXXFLAGS = $(LIBSDL_CFLAGS)
proc2iso_LDADD = $(LIBSDL_LIBS)
